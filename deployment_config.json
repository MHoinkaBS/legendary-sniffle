{
   "appName": "legendary-sniffle",
   "isDomainApp": true,
   "description": "Mule Domain Application",
   "buildPath": "legendary-sniffle",
   "buildCommand": "mvn clean install -DskipTests",
   "unitTestsCommand": "mvn test",
   "mUnitTestsCommand": "mvn clean test -Dmunit.test=",   
   "updateDatabase": true
}
